server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/flights-db
    username: appuser
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
